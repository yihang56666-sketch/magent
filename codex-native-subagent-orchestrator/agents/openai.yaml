interface:
  display_name: "Native Subagent Orchestrator"
  short_description: "Use only when the user explicitly requests bounded native subagent delegation."
  default_prompt: "Use $codex-native-subagent-orchestrator only when the user explicitly authorizes bounded native subagent delegation."

policy:
  allow_implicit_invocation: false
