# Bugfix Example

Dispatch an investigator for the relevant module and a test analyst for the
reproduction path. Give the investigator source inspection only, and the test
analyst existing tests plus safe diagnostic commands. After both report, the
main agent checks the evidence, makes the patch, and runs the regression test.
