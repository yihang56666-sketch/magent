# Code Review Example

Dispatch one reviewer for correctness and regressions and, only if warranted by
the diff, one security reviewer for trust boundaries. Scope each to changed
files and directly related call sites. The main agent verifies reported findings
against the diff before reporting review results.
