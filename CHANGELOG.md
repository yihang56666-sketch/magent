# Changelog

本文件记录 magent 的公开演进历史。它不是产品路线图：本仓库交付的是一份
Codex 原生子智能体编排 Skill（文档契约 + 无依赖结构契约测试），没有运行时
版本号。以契约测试规模与公开文档口径为准。

## [2.x] — 原生 Skill 形态（当前）

- **契约**：`SKILL.md` 入口 + `references/`（分派准则、角色目录、分派契约、
  工作流模式、原生验收案例）+ `examples/`（research / code-review / bugfix）。
- **结构契约测试**：33 项（`python -m unittest discover -s tests -v`），
  覆盖元数据、安全条款、人数映射、分派包字段、验收案例、隐私自指防护等；
  CI 在 Ubuntu / Windows 双平台运行。测试只证明文档结构一致，不证明模型
  一定遵循指令。
- **安全边界**：授权门（加载 Skill ≠ 分派许可）、只读角色、主智能体唯一
  写入、内容指纹基线（而非 git status）、等待超时与终态失败分离、显式
  close/archive 释放并发槽位。

### 演进要点（v1 运行时 → v2 Skill）

- v1.2.0 是纯本地多智能体编排运行时（26 身份库、12 协作模式、CLI + 本地
  仪表盘）。实际使用暴露了根本矛盾：执行环节是 Codex 原生线程工具已经做好的
  事，再包一层 Python 执行状态机带来持续维护的检查点、缓存与仪表盘负担。
- v2 收敛为决策、路由、安全与核验层：执行交给原生工具，仓库不再包含 CLI、
  仪表盘、模型 API 依赖或遥测（见 `AGENTS.md` 的产品约束）。
- 旧运行时归档；文档中所有旧产品承诺同步移除，避免宣传与实际内容不一致。

## [1.x] — 已归档运行时

- 26 个领域专家身份、120+ 社区导入身份、12 种协作模式。
- `route_identity.py` 多因子评分路由 + `spawn-team` / `sync` / `merge-results.py`
  脚本链 + localhost:8080 实时仪表盘。
- 全部代码已从公开仓库移除，仅保留演进记录（本节与博客复盘）。
